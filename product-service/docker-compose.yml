
services:

  ## Mongo Docker Compose Config
  mongo:
    container_name: mongo
    image: mongo:5.0.28
    restart: always
    ports:
      - "27017:27017"
    expose:
      - "27017"
    volumes:
      - ./mongo-data:/data/db